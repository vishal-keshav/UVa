#include <iostream>
#include <stdio.h>
#include <algorithm>
#include <vector>

using namespace std;

int T,N;
vector<int> input;

int main(){
cin >> T;
	while(T--){
		cin >> N;
		input.clear();
		input.resize(N);
		for(int i=0;i<N;i++){
			cin >> input[i];
		}
		
		
	}
	
	
	return 0;
}