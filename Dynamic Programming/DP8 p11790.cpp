#include <iostream>
#include <stdio.h>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
	int T,N,nr_case=1;
	cin >> T;
	while(T--){
		cin >> N;
		vector<int> height(N);
		vector<int> width(N);
		for(int i=0;i<N;i++){
			cin >> height[i];
		}
		for(int i=0;i<N;i++){
			cin >> width[i];
		}
		//Processing
		
	}
	
	return 0;
}